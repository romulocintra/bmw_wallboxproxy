def test_reference_profile_notes_are_intentionally_separate_from_runtime_alias_encoding():
    # The single-phase reference profile requires both 0x500A and 0x500C
    # current locations. Encoding of 0x500C remains independently configurable.
    assert True
