# BMW Wallbox Proxy

Home Assistant add-on packaging for a BMW wallbox Modbus meter emulator backed by Home Assistant sensor data.

Use the add-on ingress UI for the dashboard and settings page. The wallbox or TCP gateway connects to the exposed Modbus port.