# Release 0.2.29

## Fixes

- Make the PRO2 FLOAT32 encoder honor the configured `float_word_order`.
- Keep `ABCD` as the default and allow `CDAB` through the existing Home Assistant add-on option.
- Add regression coverage for both PRO2 word orders.

## Previous release

This release follows 0.2.28, which completed the PRO2 direction/quadrant register map and added the latest BMW Wallbox capture regression coverage.
